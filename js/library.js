let books=[];export async function loadLibrary(){books=await fetch('data/books.json').then(r=>r.json())}export function libraryView(){return `<section class="content"><p class="eyebrow">Legal free resources</p><h1>Knowledge Library</h1><div class="filters section"><input id="library-search" maxlength="100" placeholder="Search resources"><select id="library-category"><option value="">All categories</option>${['Cybersecurity','Programming','Networking','Mathematics','Science','Technology','General Learning'].map(x=>`<option>${x}</option>`).join('')}</select></div><div id="library" class="grid three"></div></section>`}export function renderLibrary(){const h=document.querySelector('#library'),input=document.querySelector('#library-search');if(!h||!input)return;const q=input.value.toLowerCase(),c=document.querySelector('#library-category').value,r=books.filter(b=>(!c||b.category===c)&&JSON.stringify(b).toLowerCase().includes(q));h.innerHTML=r.length?r.map(b=>`<article class="card"><span class="pill">${b.category}</span><h3>${b.title}</h3><p>${b.description}</p><a class="button secondary" target="_blank" rel="noopener noreferrer" href="${b.url}">Open resource ↗</a></article>`).join(''):'<div class="card empty">No resources match.</div>'}
