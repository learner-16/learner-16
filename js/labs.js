let labs=[];export async function loadLabs(){labs=await fetch('data/labs.json').then(r=>r.json())}export function labsView(){return `<section class="content"><p class="eyebrow">Safe practice only</p><h1>Practice Labs</h1><p class="muted">Educational exercises — never connected to real systems.</p><div class="grid three section">${labs.map(l=>`<article class="card"><span class="pill">${l.difficulty}</span><h3>${l.title}</h3><p><strong>Objective:</strong> ${l.objective}</p><p>${l.instructions}</p><button class="button" data-lab="${l.id}">Open task</button></article>`).join('')}</div><div id="lab-task"></div></section>`}export function openLab(id){const l=labs.find(x=>x.id===id),host=document.querySelector('#lab-task');if(!l||!host)return;host.innerHTML=`<article class="card section"><h2>${l.title}</h2><p><strong>Task:</strong> ${l.task}</p><p><strong>Hint:</strong> ${l.hint}</p><textarea maxlength="5000" placeholder="Write your safe learning response"></textarea><div class="actions"><button type="button" class="button" data-action="complete-lab" data-lab-id="${l.id}">Complete · ${l.score} points</button></div></article>`}
