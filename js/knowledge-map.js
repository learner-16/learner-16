import{nextTopic,state,weak}from'./learning.js';export function mapView(){const weakSet=new Set(weak().map(x=>x[0])),scores=state().scores,nodes=[['Digital Literacy','mastered','Foundation'],['Programming',weakSet.has('Programming')?'weak':'current','Prerequisite: Digital Literacy'],['Networking',weakSet.has('Networking')?'weak':'current','Prerequisite: Digital Literacy'],['Cybersecurity',weakSet.has('Cybersecurity')?'weak':'current','Prerequisite: Networking'],['Cloud Security','current','Prerequisite: Cybersecurity']];return `<section class="content"><div class="heading"><div><p class="eyebrow">Connected understanding</p><h1>Knowledge Map</h1><p class="muted">Recommended next topic: <strong>${nextTopic()}</strong></p></div></div><article class="card">${nodes.map(([n,s,p])=>`<div class="node ${s}"><span class="icon">${s==='mastered'?'✓':s==='weak'?'!':'○'}</span><div class="grow"><strong>${n}</strong><br><small class="muted">${p}</small></div><span class="pill ${s==='mastered'?'done':s==='weak'?'warn':''}">${s==='mastered'?'Mastered':(scores[n]||0)+'%'}</span></div>`).join('')}</article></section>`}
